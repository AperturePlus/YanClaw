# 特殊情况备忘录

北航有一个 shi.buaa.edu.cn 是教师主页合集，但是这里面的信息和学院里的教师主页信息可能有交集，也可能没有交集，对于同一个老师必须在两个地方（所在学院，和shi.buaa.edu.cn）都爬了，才能获取比较完整的信息。应该引入基于图的deep miner 根据已有信息深度挖掘，全力补全。

北航外国语学院老师的主页，有的用PDF展示信息，需要添加对PDF的额外处理。

原因是 学院页抽取结果用了错误 JSON 键，代码把它当成空结果丢了。
日志里 https://www.uestc.edu.cn/xybm/jxkydw_yjjg.htm 这一页其实识别出了学院，比如“信息与通信工程学院”“电子科学与工程学院”等，但 LLM 返回的是：
included_org_units: [...]
而普通院系抽取代码只认：
org_units: [...]
对应代码在 agent_parsing.py (line 158)：org_units_from_result() 只读取 payload.get("org_units")，所以 included_org_units 被视为没有抽到任何院系。于是这些学院没有进入 validated_units，也没有写入 org_units 表。
为什么科研机构能入库：后面 https://www.uestc.edu.cn/xybm/gjjkyjg.htm 先返回了 org_units 格式，代码成功解析并校验；随后二次 org-unit filter 又返回 included_org_units/excluded_org_units，这次是过滤阶段的合法格式，所以国家级科研机构被保留并入库。
当前库也能印证这一点：uestc.edu.cn.db 的 org_units 只有 9 个，全部是 research_institute，没有计算机学院、医学院等学院；教授也只入了“通信抗干扰全国重点实验室”一批。
根因链条是：
EXTRACT_ORG_UNITS 阶段加载了 org-unit-filter skill。
这个 skill 也适用于 EXTRACT_ORG_UNITS，并写了过滤任务输出格式 included_org_units/excluded_org_units。
LLM 在普通抽取 jxkydw_yjjg.htm 时受该 skill 干扰，返回了过滤格式。
抽取解析器只接受 org_units，导致学院全丢。
后续只剩国家级科研机构进入 FIND_FACULTY_PAGES 队列，所以看起来“一个学院都没入库”。
建议修复方向：把普通 org-unit 抽取和二次 filter 的 skill/格式隔离，或者让 _org_units_from_result() 兼容 included_org_units 作为兜底。更稳的是两者都做：抽取 prompt 强约束只返回 org_units，解析器兼容一次，避免同类跑批再整批丢学院。